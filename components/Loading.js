import Link from "next/link";
import styled from "styled-components";

const Main = styled.main`
  display: grid;
`;

export default () => {
  return (
    <Main>
      <h1>Aan het laden...</h1>
    </Main>
  );
};
